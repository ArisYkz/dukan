# syntax=docker/dockerfile:1

FROM ghcr.io/supabase/cli:latest

WORKDIR /app
COPY supabase ./supabase

EXPOSE 54321

# Serves all edge functions from supabase/functions
CMD ["supabase", "functions", "serve", "--workdir", "/app/supabase", "--no-verify-jwt", "--host", "0.0.0.0", "--port", "54321"]
